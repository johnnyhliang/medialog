node_modules
dist
.env.local
.DS_Store
.vite
*.local
import/*.md
import/*.txt
import/*.html
import/*.htm
import-preview.json
.aider*

# tooling state and logs — machine-generated churn
.omx/
supabase/.temp/

# session exports — local showcase artifacts, not repo content
medialog-session-export.md
medialog-session-highlights.md
session-log-*.md
distilled/

# Agent worktrees (own git repos + node_modules)
.claude/worktrees/
CLAUDE.md

# Personal operating doc: a live weekly schedule with physical locations,
# church/gym times, and recruiting status. This repo is public — keep it local.
# Paste it into a Claude Desktop Project instead of committing it.
docs/operating-system.md
