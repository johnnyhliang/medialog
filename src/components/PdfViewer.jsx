// Stub — full implementation added in Task 4
export default function PdfViewer({ url }) {
  return <p className="muted">PDF preview coming soon. <a href={url} target="_blank" rel="noreferrer">Open in new tab</a></p>
}
